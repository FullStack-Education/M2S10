import { ConversorTemperaturaPipe } from './conversor-temperatura.pipe';

describe('ConversorTemperaturaPipe', () => {
  it('create an instance', () => {
    const pipe = new ConversorTemperaturaPipe();
    expect(pipe).toBeTruthy();
  });
});
